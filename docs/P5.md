# P5 联调说明

## 当前状态

`P5` 已完成。

当前仓库已经完成了一次真实端到端联调，服务端和客户端脚本都已验证可正常工作。

已确认通过：

- `bash scripts/run_server.sh` 可以正常启动服务
- `bash scripts/run_client.sh` 可以正常发起请求
- 服务能够返回模型响应
- `REQUEST_MODE=path` 和 `REQUEST_MODE=base64` 两种模式都已具备可用调用链路

## 当前脚本能力

### 服务端脚本

[`run_server.sh`](/Users/rkos/Workspace/vlm_server/scripts/run_server.sh) 当前支持：

- 从 `.env` 读取配置覆盖
- 默认监听 `127.0.0.1:8972`
- 默认模型 `Qwen3.5-27B`
- 默认模型路径 `/home/user/g00806422/data/weight/Qwen3.5-27B`
- 默认两卡运行
- 通过 `GPU_IDS` 或 `CUDA_VISIBLE_DEVICES` 指定 GPU
- 通过 `TENSOR_PARALLEL_SIZE` 控制并行卡数

当前默认值：

- `GPU_IDS=4,5`
- `CUDA_VISIBLE_DEVICES` 默认跟随 `GPU_IDS`
- `TENSOR_PARALLEL_SIZE=2`
- `HOST=127.0.0.1`
- `PORT=8972`
- `MODEL_NAME=Qwen3.5-27B`
- `MODEL_PATH=/home/user/g00806422/data/weight/Qwen3.5-27B`
- `INFERENCE_BACKEND=vllm`
- `SKIP_MODEL_LOAD=false`

### 客户端脚本

[`run_client.sh`](/Users/rkos/Workspace/vlm_server/scripts/run_client.sh) 当前支持：

- 默认请求服务 `http://127.0.0.1:8972`
- 默认使用 `REQUEST_MODE=path`
- 支持 `REQUEST_MODE=path`
- 支持 `REQUEST_MODE=base64`
- 默认把 `readyz` 结果写到 `tmp/readyz.json`
- 默认把推理响应写到 `tmp/client_response.json`

当前默认值：

- `SERVER_URL=http://127.0.0.1:8972`
- `MODEL_NAME=Qwen3.5-27B`
- `REQUEST_MODE=path`
- `IMAGE_PATH=/home/user/g00806422/embodiment/vlm_server/tmp/1.png`
- `PROMPT=请描述这张图片中的主要内容。`
- `MAX_TOKENS=256`
- `TEMPERATURE=0.1`

## 当前推荐调用方式

### 1. 启动服务

```bash
conda activate vllm
bash scripts/run_server.sh
```

默认就是两卡模式，等价于：

```bash
conda activate vllm
CUDA_VISIBLE_DEVICES=4,5 TENSOR_PARALLEL_SIZE=2 bash scripts/run_server.sh
```

如果要切换到别的两张卡，例如物理 6 和 7 号卡：

```bash
conda activate vllm
CUDA_VISIBLE_DEVICES=6,7 TENSOR_PARALLEL_SIZE=2 bash scripts/run_server.sh
```

如果要临时切回单卡：

```bash
conda activate vllm
CUDA_VISIBLE_DEVICES=0 TENSOR_PARALLEL_SIZE=1 bash scripts/run_server.sh
```

### 2. 发起路径图片请求

```bash
IMAGE_PATH=/path/to/example.png bash scripts/run_client.sh
```

### 3. 发起 base64 图片请求

```bash
REQUEST_MODE=base64 IMAGE_PATH=/path/to/example.png bash scripts/run_client.sh
```

## 当前产物位置

- 健康状态响应：[`tmp/readyz.json`](/Users/rkos/Workspace/vlm_server/tmp/readyz.json)
- 客户端响应：[`tmp/client_response.json`](/Users/rkos/Workspace/vlm_server/tmp/client_response.json)

如果这些文件在某次执行中不存在，通常说明脚本执行中途失败，需要结合终端输出排查。

## 后续关注点

联调已经通过，但当前仍然属于 MVP 阶段，后续更值得优先补的是：

- 请求级日志和 `request_id`
- 超时控制
- 单卡串行 / 低并发保护
- 更稳定的错误结构
- 本地路径白名单和输入安全边界
