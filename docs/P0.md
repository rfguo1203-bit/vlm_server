# P0 结论与所需服务器信息

## P0 已确认结论

以下内容已经定下来，后续开发默认按这个执行：

- 部署位置：云端服务器
- 本地开发机用途：只做代码开发、静态检查、轻量接口校验，不做 LLM 推理调试
- 服务形态：简单自用版，不追求高并发
- 默认接口形式：本机 HTTP
- 默认监听地址：`127.0.0.1:8000`
- 默认服务框架：`FastAPI + uvicorn`
- 默认推理后端优先级：
  - 第一选择：`vLLM`
  - 备选方案：`transformers`
- 默认并发策略：串行或极低并发，第一版按 `1` 个推理槽位设计
- 模型：`Qwen3.5-27B`
- 图片输入：同时支持 `本机路径` 和 `base64`
- 调用方式：只允许同机进程调用

## 已确认的服务器信息

### 1. 模型路径

```text
/home/user/g00806422/data/weight/Qwen3.5-27B
```

### 2. Python 与环境管理

```text
Python 3.11.14
conda env: vllm
```

### 3. 推理依赖现状

- `torch`
- `transformers`
- `vllm`
- `fastapi`
- `uvicorn`

```text
torch 2.10.0
transformers 4.57.6
vllm 0.15.1
fastapi 0.128.2
uvicorn 0.40.0
```

### 4. CUDA / GPU 信息

```text
Driver Version: 580.65.06
CUDA Version: 13.0
GPU: NVIDIA H100 80GB HBM3
Memory: 81559 MiB
```

### 5. 服务运行方式

```text
服务监听: 127.0.0.1:8000
调用方式: 同机进程调用
图片输入: 本机路径 + base64
```

## P0 完成标准

当以下两件事都明确后，`P0` 就算完成：

- 上面的默认决策不再变
- 服务器环境信息已经收齐，可以落到配置文件

## P0 状态

`P0` 已完成。

后续默认按以下实现前提推进：

- 云端服务器 conda 环境：`vllm`
- Python：`3.11.14`
- 推理后端优先：`vllm 0.15.1`
- 模型目录：`/home/user/g00806422/data/weight/Qwen3.5-27B`
- 服务监听：`127.0.0.1:8000`
- 输入方式：本机路径 + base64
